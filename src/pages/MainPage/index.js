import React from 'react';
import {
	MainPageContainer,
	SettingsSection
} from './mainPageStyles';

const MainPage = () => {
	return (
		<MainPageContainer>
			<SettingsSection>

			</SettingsSection>
		</MainPageContainer>
	);
};

export default MainPage;
